# 貼上即用：要加去 `playerkousas-rgb/readme` 真理倉嘅加註

> **點解要你落手**：我對 `playerkousas-rgb/readme` **冇 push 權限**（`gh api repos/playerkousas-rgb/readme --jq .permissions` → admin／maintain／push 全 `false`），開唔到 PR。
> 下面每一格係獨立一段，複製去對應檔案嘅對應段落尾就得（用 `> ` 引用樣式，唔會蓋住原文）。
> 來源：旅側實作 ＋ 你 2026-09-25 嘅四條定案（詳見 `docs/旅系統建構計劃.md` 附錄 D、`docs/落差報告.md` §3）。

---

## ① `BUILD.md` §5「分享」——收件方決定（保留）

```markdown
> **旅側加註（2026-09-25 定案）**：內部分享除咗「發佈時揀支部」，同時保留**收件方決定** ——
> 發起方一次揀（可多選）對象；**每個收件支部各自決定接收／退回**（退留理由、記邊個決定）。
> **接收先出現**（未接收只喺「待接收」），**唔會自動混入**收件方清單。
> 例：深資團發活動邀請 → 童軍團領袖唔想小朋友參與，退回就得。
> 目前 UI 只做 **通告 ＋ 活動（行事曆）**；物資／進度／相簿／教材唔開（資料欄留住）。
```

## ② `BUILD.md` §2「災難恢復」——DELETE ACCOUNT 保最小 1 個

```markdown
> **旅側加註（2026-09-25 定案）**：「災難恢復」指 **刪除帳號要保留最小 1 個** ——
> 每個 leaf（旅 SHEET／每團）**至少留 1 個領袖戶**；刪／停用之前會過下限守衛，刪淨最後一個會被擋。
> 刪除會留紀錄（邊個、幾時、邊個做）。平台超管（隱藏）唔可以停用／刪除（第二層備援）。
> ⚠ 呢條**唔等於**保留本地登入通道：閂咗支部系統登入（`sig-only`）之後，本地領袖戶一樣 `403`，
> 出入只經旅入口 —— 屬預期，唔算災難。
```

## ③ `BUILD.md` §1「入口開關」——下游側冇控制掣（求救制補位）

```markdown
> **旅側加註（2026-09-25 定案）**：支部側**冇任何控制掣**（唔設原因欄、唔設逃生門／救援碼），
> 只有一條免登入 **`🆘 求救`**（`index.html?step=rescue&b=<id>`）＝送去旅側 ADMIN 嘅**請求**。
> 旅側 ADMIN 喺「待辦與批核 → 🆘 求救」人手核實身份後處理：① 開返支部系統登入 ② 重設密碼 ③ 答覆並結案。
> **求救只係請求，唔會自動開任何嘢**；求救亦唔會改動下游 `ALLOW_LOCAL_LOGIN`（唯一保留＝文件 fallback：
> 下游 Sheet 直接刪 `ALLOW_LOCAL_LOGIN`，唔做 UI）。
> 兩態：`open`（支部都可以自己登入）／`sig-only`（只經旅入口）。**冇「標準狀態」、冇「連旅入口都擋」呢個狀態。**
> 落閂前有**前置檢查**（見下）。
```

## ④ `進度追蹤旅系統升級版.md` §12④——閂口前置檢查

```markdown
> **旅側加註（2026-09-25 定案＋已落碼）**：閂口前自動列**前置檢查** 4 項：
> ① 接駁燈綠（已接駁）② 測試連線成功（有 `testedAt`）③ 進度下游已登記（有 `progressSource`）
> ④ 帳號下限提示（該 leaf 仲有領袖戶）。**未達標出警告，但唔硬擋**（仍可閂）；
> 審計會記低前置檢查結果。對應原文精神：「先搬數、先測連線，先至閂口」。
```

## ⑤ 超管第二層（`BUILD.md` §1／§7 旁邊）

```markdown
> **旅側加註（2026-09-25 定案＋已落碼）**：平台超管唔經下游登記 Sheet 驗身 ——
> **任何支部（未登記／紅燈／閂咗閘／接駁斷）＋全部模組都入得**，作為第二層備援。
> 身份**隱藏**：唔出現喺名單／統計／邀請／導航；每頁紅色橫額提醒日常唔插手。
> 救命範圍：開返支部系統登入、重設 ADMIN 密碼。（第一層＝支部 🆘 求救 → 旅側 ADMIN。）
```

## ⑥ `BUILD.md` §5「公開頁」——旅側維持免登入

```markdown
> **旅側加註（2026-09-25 定案）**：旅公開頁（`public.html`）**維持免登入**（旅公開資料 lvl0），
> **唔跟**「公開頁要登入＋公開帳共享密碼」。要對外嘅內容由旅長上報／批核之後先出（lvl0）。
```

---

## ⑦–⑫ 已定案（2026-09-26）——可以加註

```markdown
> **旅側加註（2026-09-26 定案）**：
> ⑦ **成員登入＝混合制** —— 未接駁嘅團行 M3（旅側只做門戶，轉去該團入口驗密碼）；
>    「已登記＋綠燈＋測過連線」嘅團自動出 M1（旅側喺旅閘收密碼後即刻經 `sig` 交該團驗，**旅唔存密碼**）。
>    成員帳號密碼 hash 一律住**該團 SHEET**（§2 錨點）；改好一個團升一個團，團側零改動都照跑得。
> ⑧ **旅 ID ＝ 4 位補零**（`82` → `0082`），跟現行 `normId` 單一實現（trim → 大寫 → 數字補零 4 位＋字母尾）。
> ⑨ **家長綁定子女＝該團領袖確認先睇到**（唔開放自助即時生效）；唔使上載證明，對名冊即可；
>    一個子女可以綁多過一個家長（各自申請、各自核實）。
> ⑩ **整合數據新鮮度＝分層 cache**：通告／活動／點名 5 分鐘；財務／物資／進度 30 分鐘；＋「強制刷新」清 cache；
>    寫入／批核即清相關 cache。
> ⑪ **`TROOP_MODULES` ＝旅 SHEET「模組開關」分頁為真相**（改一次寫一次＋入 `AUDIT_LOG`）＋ 5 分鐘快取；
>    改得嘅只有旅長；**閂咗模組＝隱藏，唔刪資料**。
> ⑫ **零回打版（`cubsbadge`／`scoutbadge`）＝已知例外，列 backlog**；旅系統 P0 起好之後一次過拉齊回打 `/api/super`。
```

---

## ⏳ 仲要你／ADMIN 落手

| # | 做咩 | 點解唔係我做 |
|---|---|---|
| 1 | 貼上面 ①–⑥ 加註落 `readme`／`BUILD.md`／`進度追蹤旅系統升級版.md` | 我對 `readme` **冇 push 權限** |
| 2 | 貼 ⑦–⑫ 加註（同一頁，格式一樣） | 同上 |
| 3 | 想我直接改下游 repo（`vs_portal` 加 sig／`cubsbadge` 對齊／`readme` 加註）→ 喺 **Arena 重新連 GitHub** 時揀埋嗰啲 repo | 我而家 push 得 `troop_portal` 一個 |
