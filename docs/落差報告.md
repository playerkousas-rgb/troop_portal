# 落差報告：現況 vs `readme`（BUILD.md／進度追蹤旅系統升級版.md）

> 2026-09-25 · 對照真理倉 `playerkousas-rgb/readme` 最新版（`BUILD.md` 151 行、`進度追蹤旅系統升級版.md` 299 行）
> 目的：講清楚**仲有咩大項未做**、**離 BUILD 期望幾遠**、**邊啲位要你拍板／要調整**。

---

## 0. 一句話

**UI 先行嘅承諾已經交到：功能齊備度可以用 UI 逐項檢查。**
但由「UI 睇得」去到「真係用得」，仲有三個大件未起：**①旅後端（Code.gs ＋ `/api/*`）②真 `sig` 接駁 ③平台 ADMIN 收件匣**。
按 BUILD §10 嘅施工次序，呢三件**全部係後端**，唔係 UI。

---

## 1. 距離一覽（粗略百分比，計「可用」唔係「寫咗幾多行」）

| 層 | BUILD 期望 | 現況 | 距離 |
|---|---|---|---|
| **旅系統 UI（本 repo）** | 一套 UI 模版行晒（模組註冊制、掣位統一） | ✅ 16 模組 · 18 路由 · 註冊表生成導航 · lint 53 檔 · **smoke 80/80** · 真模式寫入掣已接 `/api/proxy`（連公開頁匿名面） | **~90%**（剩美化／微調） |
| **旅系統後端（Code.gs）** | `requireAuth`、action 白名單、ScriptLock、逐表寫自證、`sig` 驗簽、`AUDIT_LOG`／`ACCESS_LOG`、`dbInfo`、`exportAll`／`importAll` | ✅ **已寫好**（`apps-script/Code.gs` · 1050+ 行；**P1 已加**：大庫分件＋分段讀、tombstone＋purge、權限封頂、registry 燈號、分享／求救寫入、匿名寫入限流、`saveDbPart`）：router／apikey＋sig 雙通道驗身／白名單／永不接受清單／ScriptLock／逐表寫＋讀返自證／審計 `prev_hash` 鏈／限流／帳號下限／邀請／registry（ScriptProperties）／出站 `sig`／閂口／`dbInfo`／匯出匯入／Drive 備份／選單；**本機 38 項測試綠**（假 GAS 環境） | **~55%**（未上真 Sheet 實測） |
| **`/api/*`（Vercel）** | `/api/proxy`（注入 apikey）、`auth`、`super`（回打驗票）、`units`、cache 5 分鐘＋flush | ✅ 已寫好 **9 支**（＋`troop` 聚合／`registry` 能力表／`member-entry` 導流／`share` 簽名連結）：`proxy`（問題回報 `issue` ＋ 旅 GAS action 白名單 ＋ session 驗證 ＋ 4MB／逾時）、`auth`（**PBKDF2 100k**／session／改密碼／邀請／setup token）、`super`（AES-GCM 票據 60 秒＋綁旅＋一次性）、`units`（公開清單＋只列變數名嘅診斷）、`downstreams`（只回公開欄位＋5 分鐘 cache）；**本機 21 項測試綠** | **~50%**（未上真 Vercel 實測） |
| **真 `sig` 讀寫** | 上游→下游 HMAC 雙通道、±5 分鐘、nonce 一次性、讀／寫白名單 | ✅ 旅側實作齊（雙通道驗簽＋nonce 消耗＋時窗＋出站簽＋purpose 逐條登記＋URL 白名單）；**未同真下游對打** | **~45%** |
| **真密碼與 session** | PBKDF2 ≥100k＋salt＋timing-safe、5 次鎖 15 分鐘、`mustChangePw` gate、JWT HttpOnly ≤30 分鐘＋silent refresh | ✅ `api/auth.js`＝PBKDF2-SHA256 100,000＋per-user salt＋timing-safe／HS256 session（HttpOnly Secure SameSite=Lax 30 分鐘）／5 次鎖 15 分鐘；GAS **永不驗密碼**（未設 hash 嘅戶＝`pending_hash`）；⏳ 未上真環境、silent refresh 未做 | **~70%** |
| **團側（下游）** | `handleSignedRequest`、`DOWNSTREAM_*`、`ALLOW_LOCAL_LOGIN`、403 頁求救連結 | `vsbadge`／`roverbadge` ✅ 已 built；`vs_portal`／`cubs_portal`／`scout_portal`／`gh_portal` ❌ **未有** | **~40%**（平均） |
| **平台 ADMIN** | 收件匣 GAS、units.json ＋ Vercel env、2FA、per-IP 限流 | ✅ **P2b**：`scripts/units.mjs`（`npm run units`：list／add／update／rm／env／check；`check` 擋重複 ID 同**擋 key／URL 入檔**）＋平台頁「開旅／接入」卡（四步＋睇真 `/api/units?diag=1`）＋`docs/教材/08` 開旅 checklist 照住做；⏳ GAS 收件匣屬另一個 repo（Scout Admin），未見 | **~35%** |
| **個人化訂閱 ★** | 圖書館推送鏈（Supabase ＋ GitHub Actions ＋ VAPID ＋ service worker） | ⏳ UI 有訂閱設定；✅ 新增 `push.js`：訂閱 payload 只帶匿名維度（**冇 email／姓名**）、題材白名單、能力檢查、走圖書館原鏈（`source:'troop_portal'`）；**未接真 Supabase／VAPID** | **~45%** |
| **同步與多人寫入** | staging 黃點、版本樂觀鎖、merge3、ScriptLock 排隊、tombstone purge | ✅ 前端 `offline.js`：三色燈＋`baseVersion` 樂觀鎖＋**merge3 欄位級合併**（唔同欄各自保留／同一格衝突彈出逐格 ask／批量＝serverTime 新者勝＋留底）＋逐行合併（新增／刪除認得出）＋backoff＋jitter 重試＋離線隊列；後端：`ScriptLock` 20 秒、逐表寫＋讀返自證、**大庫分件**（自動＋`saveDbPart`）、**tombstone＋90 日 purge** | **~85%** |
| **備份** | `exportAll` 單一 JSON＋sha256、每週 Drive 留 13 份、`importAll` 冪等、三時機提醒 | ✅ GAS `exportAll`（帶 sha256 指紋）／`importAll`（保留舊 hash、冪等、≤2000 筆）／`backupToDrive`（建立即 PRIVATE）／`備份紀錄` 分頁；✅ **P2b**：13 份輪替（`backupFileName_` 自家格式先刪、13 份以外 `setTrashed`）＋`backupState` 三時機提醒（升級前／批量前／每週）＋UI 備份卡（即刻做／落本機／匯入前警告、示範＝本機紀錄）；⏳ 未上真 Drive 實測 | **~85%** |
| **體積治理** | 零依賴 `api`、`.vercelignore`、`dist<5MB`、AVIF、CI 硬攔 | ✅ 零依賴 `api`、`.vercelignore`、`vercel.json`（hkg1）；✅ 新增 `formats.js`：上載 ≤5MB／每筆 ≤3 張／每日上限／AVIF·WebP 優先＋dist<5MB·bundle<2MB 檢查（`npm run check` 就係嗰條硬線）；⏳ 未接真 CI workflow | **~85%** |

**整體（2026-09-26 · P2b 更新）**：以「功能齊備＋規格對齊」計 **~90%**；以「真用得（後端＋接駁）」計 **~56%**
（後端兩層寫好＋本機測試綠；**未上真 Sheet／真 Vercel／未同真下游對打** —— 呢三樣係剩下嘅主要距離）。

---

## 2. 大項未做（排序＝BUILD §10 施工次序）

> 2026-09-26 更新：**問題回報去 ADMIN 收件匣**已對正合同（見 §3 #10）—— `/api/proxy` 落 `action:'issue'` 就即刻通。

### ① 旅 `Code.gs`（P0 後端）—— ✅ 已寫好（2026-09-26），剩「上真 Sheet 實測」

> `apps-script/Code.gs`（833 行）＋ `scripts/gas-test.mjs`（假 Apps Script 環境 · 23 項）。詳見下方原本清單（全部已實作）。
> **下一步（人手）**：貼上 GAS → 跑 `initializeSheets()` → 選單「🔑 顯示 BACKEND／APIKEY」→ 放入 Vercel env → 用真 Sheet 行一次「測試連線」。
- `doGet`/`doPost` 首行 `requireAuth`；**key 未設定＝拒絕敏感 action**（fail closed）
- **`sig` 驗簽**：`HMAC-SHA256(action+ts+nonce+sha256(body))`、±5 分鐘、nonce 一次性（CacheService 10 分鐘）、雙通道（query 防 GAS 302／body）
- **action 白名單**（讀 12／寫 19）＋永不接受清單；`setGate`／`upsertUser`／`bulkAddUsers`／`exportAll`…
- **ScriptLock `waitLock(20s)`**、原子寫（暫存行＋swap）、逐表寫自證（寫完讀返）
- `AUDIT_LOG`（`prev_hash` 鏈）／`ACCESS_LOG`（append-only、server-side）
- `dbInfo`／`status`（`backendVersion`）、`exportAll`／`importAll`、Drive 備份＋每週排程
- 生成物唔 commit：`scripts/build-gas.mjs` 產生 `apps-script/Code.gs`

### ② `/api/*`（Vercel functions，零依賴）—— ✅ **九支已寫好**（2026-09-26 · P1 完成）

> `proxy`（`issue` ＋ 旅 GAS action 白名單 ＋ **旅長／教練員分權**）、`auth`（PBKDF2 100k／session）、`super`（票據）、
> `units`、`downstreams`、**`troop`（旅層聚合：分層 cache 5／30 分鐘＋`?fresh=1`）**、
> **`registry`（能力表：模組開關＋支部燈號＋`branch=` 逐支部答「用唔用得」）**、
> **`member-entry`（M1／M3 導流；唔做帳號枚舉）**、**`share`（簽名連結：通告／活動、最長 90 日、過期自動失效）**。
> 本機 18 項測試綠。
- `/api/proxy`：驗 session → 注入 apikey → 打旅 `/exec`
- `/api/auth`（登入／登出／改密碼）、`/api/super`（**固定受信端點回打驗票**：60 秒票據、AES-GCM、一次性防重放）
- `/api/units`、`/api/downstreams`、cache 5 分鐘＋flush endpoint
- Session：JWT HttpOnly `Secure; SameSite=Lax`、≤30 分鐘＋silent refresh

### ③ 真密碼與安全
- PBKDF2-SHA256 ≥100k＋per-user salt＋timing-safe compare；舊 hash 首登自動升級
- 每帳號 5 次失敗鎖 15 分鐘；`mustChangePw` 期間只放行改密碼 API；改密碼 `pv+1`
- 忘記密碼（email 一次性連結）、邀請連結（12 字元、24 小時）
- 秘密掃描測試（掃死後門／舊 hash）

### ③b 已補（P1 · 2026-09-26）

- ✅ **大庫分件**：`saveTable`／`saveTables` 超 20,000 行自動切件（`旅通告#1`…），`load`／`loadTables` 自動合返；縮返細會刪走分件
- ✅ **`saveDbPart`**：前端主動分件寫（part 0＝主分頁＋`reset`）
- ✅ **tombstone**：`deleteRow` 寫墓碑（邊個／幾時／為咩）＋`getTombstones`；`purgeTombstones` 90 日清理
- ✅ **權限封頂**：`updateUserRole`／`updatePermissions` 唔可以授唔低過自己嘅角色；權限只可以授自己角色有嘅，超出＝**如實回報 `clamped`**
- ✅ **匿名寫入限流**：求救（免登入）每 IP 每個鐘 ≤3 單；落 `操作紀錄`
- ✅ **前端**：`offline.js`（三色燈／樂觀鎖／backoff＋jitter／離線隊列）、`push.js`（匿名訂閱）、`formats.js`（上載＋體積）
- ✅ **UI**：通告詳情「簽名分享連結」、行事曆「訂閱日曆／活動分享」、求救真模式落旅 SHEET

### ④ 真 `sig` 接駁
- 旅側落碼後，用 **`vsbadge`／`roverbadge`（已 built）** 做首個實測：登記 → 測試連線 → 開戶 → 搬數 → 閂口
- 其他團照 §4.8 清單補（`handleSignedRequest`／`DOWNSTREAM_*`／`ALLOW_LOCAL_LOGIN`／`SUPER_VERIFY_URL`／守護測試）
- **閂口次序鐵律**（升級 MD §12④）：**先搬數、先測連線，先至閂口**；而且「未起好進度 sig 路徑之前唔好閂口」

### ⑤ 同步與多人寫入
- 版本由 server 派（ISO＋隨機尾數）、寫入帶 `baseVersion` 樂觀鎖、撞版回 `conflict:true`
- merge3 欄位級合併（同格衝突逐格確認）；ScriptLock busy → 前端排隊重試（backoff＋jitter）
- 大庫分件（`saveDbPart`）＋分段讀；刪除＝tombstone，後端定期 purge

### ⑥ 個人化訂閱 ★（BUILD 寫「重中之重」）
- 前端訂閱設定 → **圖書館現有推送鏈**（Supabase `push_subscriptions` → GitHub Actions 06:00 `notify.py` → pywebpush）
- 要圖書館側改：`require_same_origin` 加 origin allowlist；`push_subscriptions` 加 `source` 欄
- 通告頁＝本單位通告＋已訂閱圖書館通告（同頁同列表、來源標示）

### ⑦ 平台與教材
- ADMIN 收件匣 GAS（只服務獨立前端接入、per-IP 限流、2FA）＋ units.json／Vercel env 流程
- 教材三層（角色快速入門／模組說明／MOCK 引導任務）＋「開旅 checklist」

### ⑦b 匿名可寫面（BUILD §3）—— ✅ 已全套落碼（2026-09-26）

> 每種都係「**白名單 action ＋ 限流 ＋ 寫入待批表**」：
> 通告報名（同通告同名去重）／物資借用（同人同件同日）／收支申報（同支部同期）／進度申報（同支部同期）／
> 開戶申請（**同 YMIS 待批唯一**）／求救（免登入，每 IP 每鐘 3 單；申請類 10 單，**分開計**）。
> 領袖決定：`decideApplication`（批／拒；**拒一定要寫原因**）；批開戶 ＝ **即回一次性邀請 token**；
> 純邀請制 `setApplyMode`（求救唔受影響）。測試：GAS 34 項內含 3 組匿名面／去重／批核。

### ⑨ 教材三層（BUILD §9）—— ✅ 已落 `docs/教材/`（跟版本走）

> 角色快速入門 ×5（旅長／教練員／家長／支部人員／平台超管）＋模組說明＋MOCK 引導任務＋開旅 checklist（A–F）＋開戶與批核；
> UI「系統 → 教學」有同一份對照表（`DOC_FILES`），smoke 會驗檔案真係存在（唔可以有死連結）。

### ⑧ 未做嘅功能模組（UI 已有目錄／部分）
- **相簿**：上載（單檔 ≤5MB、每筆 3 張、每日上限）、公開發佈要家長同意確認 —— 用戶已定案**今輪唔開**
- 進度頁（B 模式打進度 `/exec`）—— 屬支部系統；旅側只留接入位
- 財務／物資「真提交」（支部用自己 key 簽）—— 目前旅側有 UI 同示範流程

---

## 3. 同 BUILD 對唔上／要你拍板嘅位（你話「我知道有些地方要調整」）

| # | BUILD／升級 MD 寫法 | 我哋而家 | 要點 |
|---|---|---|---|
| 1 | **公開頁要登入先入到**（＋公開帳共享密碼，冇戶外人入唔到） | `public.html` **免登入**（旅公開資料 lvl0） | ✅ **定案（2026-09-25）：維持免登入** —— 用戶：「你而家果個都 OK」。唔跟 BUILD「要登入＋公開帳」；BUILD 文件加註 |
| 2 | **每個 leaf 至少留一本地領袖戶＋SUPER 作災難恢復** | 求救制；**本地領袖戶／SUPER 本地戶一樣 403** | ✅ **定案（2026-09-25）：指 DELETE ACCOUNT 要保留最小 1 個** —— 唔係要保留本地登入通道。已落碼：`removalGuard()` 擋「刪／停用最後一個領袖戶」；`removeUserAccount()` 留紀錄；超管（隱藏）刪唔到 |
| 3 | **閂口次序**：先搬數、先測連線，先至閂口；「未起好 sig 路徑之前唔好閂口」 | 閂閘只需確認（有求救提示） | ✅ **定案＋已落碼（2026-09-25）：加前置檢查**（接駁燈綠／測試連線成功／進度下游已登記＋帳號下限提示）；未達標**出警告、唔硬擋**；審計記低前置結果（`gatePreflight()`／`applyGate()`） |
| 4 | **內部分享＝發佈時揀支部** | 收件方決定（接收先出現） | ✅ **定案＋已落碼（2026-09-25）：兩者並存** —— 用戶：「係我發你可以唔收，我想深資活動想童軍支部參與姐，我童軍團領袖唔想比仔參與唔得咩?」發起方一次揀（多選）對象，每個收件支部獨立收／退；維持「接收先出現」。`readme` 加註：**我冇 push 權限**，文字已備（建構計劃附錄 D），要你／ADMIN 貼 |
| 5 | **`TROOP_OPS` 表唔建**（Registry 只落 ScriptProperties） | 一致 ✅ | 冇落差，記錄備查 |
| 6 | **零回打版**（cubsbadge／scoutbadge）中央登入用 Vercel 驗 `SUPER_KEY` | 示範資料已標「（零回打版）」 | 對齊時改跟 ① 回打 `/api/super` |
| 7 | **成員登入**：BUILD 三點進入（sig／本地／SUPER） | UI 行 M3（旅只帶你去該團入口） | ✅ **定案（2026-09-26）：混合** —— M3 做底；已登記＋綠燈＋測過連線嘅團自動 M1（一次登入，旅側經 sig 交該團驗）；密碼 hash 住該團 SHEET。已落碼：`loginRouteFor()`＋支部登入頁顯示 |
| 8 | **進度頁屬支部系統**（B 模式） | 旅側有「進度摘要」tab（純示範） | 接入要等團側；旅側唔做進度 UI（BUILD 豁免）✅ |
| 9 | **`permissions_override` 封頂**（下級 ⊆ 上級） | 逐人 `perms`（rank／借用／公開上報）已做，未做封頂驗證 | 後端落碼時一齊做 |
| 10 | **問題回報去 ADMIN 收件匣**（`BUILD` §7 ADMIN APP 收件匣） | 舊寫法係 `kind:'issue'` ＋ `message` ⇒ 同 scout-admin「問題回報 TICK」**格式唔對**（落到 ADMIN 係一堆冇名欄位，同圖書館 report.html 唔通同一張表） | ✅ **已對正（2026-09-26）**：`type:'issue'` ＋ `sourceApp:'troop_portal'` ＋ `title/desc/severity/troopId/name/contact`；嚴重度白名單（亂填落「高」）；經 `/api/proxy` 由 server 側送；**ADMIN 側零改動** |

---

## 4. 等你答（唔答我就照「建議」落碼）

> **2026-09-25 進度**：第 3 節表嘅 #1–#4 已定案並落碼（見 ✅ 行）；下面 §13 六條仍未答。

**另加（2026-09-26 用戶指正）**：**問題回報 TICK 合約已對正**（第 3 節表 #10）—— 求救表＝標題＋嚴重度＋問題詳情，格式同 scout-admin `report.html` 完全一樣；ADMIN 收件匣唔使改任何嘢；示範模式只入本機＋審計（前端零 fetch）。

**§13 六條 —— ✅ 全部已定案（2026-09-26 用戶答）**（決策過程：`docs/13-決策簡報.md`）：
1. 成員登入路線：**混合**（M3 做底；接駁好嘅團自動 M1 一次登入）＋密碼 hash 住該團 SHEET
2. 旅 ID 格式：**4 位補零**（`normId` 單一實現）
3. 家長綁定子女：**該團領袖確認先睇到**（唔使上載證明；一個子女可以綁多過一個家長）
4. 整合數據新鮮度：**分層 cache**（通告／活動／點名 5 分鐘；財務／物資／進度 30 分鐘）＋強制刷新
5. `TROOP_MODULES` 存邊：**旅 SHEET 分頁＝真相＋5 分鐘快取**（只限旅長改；閂咗＝隱藏唔刪）
6. `readme` PR：**列 backlog**（零回打版例外先記住；P0 之後一次過拉齊）

> 六條之中，UI／示範層做得到嘅（登入路線顯示、分層 cache＋強制刷新、模組開關說明、normId 驗證）**已落碼**；
> 真後端部分（`sig` 派票、cache 實作、分頁寫入）等 `Code.gs` P0。

**第 3 節表嘅三條（公開頁／災難恢復／前置檢查／分享）已於 2026-09-25 定案，見上表 ✅ 行；落碼已完成（`check` 全綠＝lint 35 檔 · 697 KB ／ smoke 60/60）。**

---

## 5. 我建議嘅次序（下一輪）

1. ~~你答上面 §13 六條~~ ✅ **已全部定案（2026-09-26）**；UI 層已落碼，`check` 全綠＝lint 35 檔 · 706 KB ／ smoke **64/64**
   - ★ **`readme` PR**：我冇 push 權限（`permissions` 全 false）→ 加註文字**貼上即用版**：`docs/readme-加註文字.md`（另見建構計劃附錄 D），請你或 ADMIN 貼上
2. **旅 `Code.gs` P0**（`requireAuth` → `sig` 驗簽 → 白名單 → ScriptLock → 逐表寫自證 → AUDIT）＋ `build-gas.mjs`
3. **`/api/*`**（proxy／auth／super／units／downstreams）＋ PBKDF2＋session
4. **真 `sig` 實測**（用 vsbadge／roverbadge）→ 打開路由 S；順手補閂口前置檢查
5. **個人化訂閱**（★）＋ Drive 備份／排程
6. 平台收件匣（另一個 repo）＋教材三層

> 之後每次落碼照舊：先改本計劃（同呢份落差報告），再落碼；`npm run check` 要綠。
