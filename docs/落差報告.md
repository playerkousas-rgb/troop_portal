# 落差報告：現況 vs `readme`（BUILD.md／進度追蹤旅系統升級版.md）

> 2026-09-25 · 對照真理倉 `playerkousas-rgb/readme` 最新版（`BUILD.md` 151 行、`進度追蹤旅系統升級版.md` 299 行）
> 目的：講清楚**仲有咩大項未做**、**離 BUILD 期望幾遠**、**邊啲位要你拍板／要調整**。

---

## 0. 一句話（2026-09-26 · P15 更新）

**框架初構＝完成**（本 repo 之內做得到嘅都做完）：UI（16 模組）＋旅後端 `Code.gs`（53 項本機測試）＋
`/api/*` **10 支**（**27 項**本機測試）＋下游範本 `Downstream.gs`＋推送收件閘 `api/push.js`＋CI 硬攔＋
`npm run preflight` 上線自檢；`npm run check` exit=0（lint 59 檔 · 1063 KB ／ GAS 53 ／ api 27 ／ smoke **95/95**）。
**移位嘅係：全部剩餘項目都喺本 repo 之外** —— ①ADMIN 人手部署＋真環境驗收（`docs/後端部署步驟.md` §5）②各下游 portal 抄範本 ③館方開通 Supabase／VAPID 推送鏈。

---

## 1. 距離一覽（粗略百分比，計「可用」唔係「寫咗幾多行」）

| 層 | BUILD 期望 | 現況 | 距離 |
|---|---|---|---|
| **旅系統 UI（本 repo）** | 一套 UI 模版行晒（模組註冊制、掣位統一） | ✅ 16 模組 · 18 路由 · 註冊表生成導航 · lint 59 檔 · **smoke 95/95** · 真模式寫入掣已接 `/api/proxy`（連公開頁匿名面）· 教材 1–13 | **~98%**（剩美化／微調） |
| **旅系統後端（Code.gs）** | `requireAuth`、action 白名單、ScriptLock、逐表寫自證、`sig` 驗簽、`AUDIT_LOG`／`ACCESS_LOG`、`dbInfo`、`exportAll`／`importAll` | ✅ **已寫好**（`apps-script/Code.gs` · 1050+ 行；**P1 已加**：大庫分件＋分段讀、tombstone＋purge、權限封頂、registry 燈號、分享／求救寫入、匿名寫入限流、`saveDbPart`）：router／apikey＋sig 雙通道驗身／白名單／永不接受清單／ScriptLock／逐表寫＋讀返自證／審計 `prev_hash` 鏈／限流／帳號下限／邀請／registry（ScriptProperties）／出站 `sig`／閂口／`dbInfo`／匯出匯入／Drive 備份／選單；**本機 53 項測試綠**（假 GAS 環境） | **~55%**（未上真 Sheet 實測） |
| **`/api/*`（Vercel）** | `/api/proxy`（注入 apikey）、`auth`、`super`（回打驗票）、`units`、cache 5 分鐘＋flush | ✅ 已寫好 **9 支**（＋`troop` 聚合／`registry` 能力表／`member-entry` 導流／`share` 簽名連結）：`proxy`（問題回報 `issue` ＋ 旅 GAS action 白名單 ＋ session 驗證 ＋ 4MB／逾時）、`auth`（**PBKDF2 100k**／session／改密碼／邀請／setup token）、`super`（AES-GCM 票據 60 秒＋綁旅＋一次性）、`units`（公開清單＋只列變數名嘅診斷）、`downstreams`（只回公開欄位＋5 分鐘 cache）；**本機 27 項測試綠** | **~50%**（未上真 Vercel 實測） |
| **真 `sig` 讀寫** | 上游→下游 HMAC 雙通道、±5 分鐘、nonce 一次性、讀／寫白名單 | ✅ 旅側實作齊（雙通道驗簽＋nonce 消耗＋時窗＋出站簽＋purpose 逐條登記＋URL 白名單）；**未同真下游對打** | **~45%** |
| **真密碼與 session** | PBKDF2 ≥100k＋salt＋timing-safe、5 次鎖 15 分鐘、`mustChangePw` gate、JWT HttpOnly ≤30 分鐘＋silent refresh | ✅ `api/auth.js`＝PBKDF2-SHA256 100,000＋per-user salt＋timing-safe／HS256 session（HttpOnly Secure SameSite=Lax 30 分鐘）／5 次鎖 15 分鐘／**P3 靜默刷新**（剩 <10 分鐘自動續期、滑動上限 8 小時、過期或改簽一律 401 `reauth_required`；前端讀 `troop_exp` 排程，收到 401 會續期一次再重試）；GAS **永不驗密碼**（未設 hash 嘅戶＝`pending_hash`）；⏳ 未上真環境 | **~80%** |
| **團側（下游）** | `handleSignedRequest`、`DOWNSTREAM_*`、`ALLOW_LOCAL_LOGIN`、403 頁求救連結 | ✅ **P12**：本 repo 提供**可直接抄嘅範本** `apps-script/Downstream.gs`（sig 驗簽雙通道＋時窗＋jti 持久環防重放／`ALLOW_LOCAL_LOGIN` 閂口＋403 求救指引／`never_accept` 本地憑證操作／**leaf 自製 session token** 5–30 分鐘、綁 pv＋node＋key）＋教材 12＋`doPost` 三步掛勾；`vs_portal`／`cubs_portal`／`scout_portal`／`gh_portal` **各自複製範本即接**（本 repo 唔代人改其他 repo） | **~75%**（範本已齊＋測試釘死；等各 portal 抄） |
| **平台 ADMIN** | 收件匣 GAS、units.json ＋ Vercel env、2FA、per-IP 限流 | ✅ **P2b**：`scripts/units.mjs`（`npm run units`：list／add／update／rm／env／check；`check` 擋重複 ID 同**擋 key／URL 入檔**）＋平台頁「開旅／接入」卡（四步＋睇真 `/api/units?diag=1`）＋`docs/教材/08` 開旅 checklist 照住做；⏳ GAS 收件匣屬另一個 repo（Scout Admin），未見 | **~35%** |
| **個人化訂閱 ★** | 圖書館推送鏈（Supabase ＋ GitHub Actions ＋ VAPID ＋ service worker） | ✅ **P14 前端＋收件閘完成**：`sw.js`（收 push／notificationclick 開返 APP／`pushsubscriptionchange` 叫人重新訂閱；**唔攔 fetch**）、`push.js` **真訂閱四步**（能力檢查→同源 `/api/push` 拎設定→權限→`pushManager.subscribe` VAPID）；`/api/push`＝**轉發閘**（白名單剝 PII：endpoint／keys／topics／scope／source、限流 10 次／10 分鐘、Bearer 去館方收件位）；**未設 `VAPID_PUBLIC_KEY`／`PUSH_INGEST_URL`＝誠實講未開通**（唔會扮成功、唔會自己存訂閱表）；UI 顯示能力／鏈狀態／四步結果；教材 13。**剩**：ADMIN 設 env ＋ 館方 Supabase 收件位部署（館方側） | **~80%** |
| **移交與升降團（§6）** | 移出 tombstone／套裝 JSON＋sha256／接收（冪等＋撞號）／家長處理／升團季批量 | ✅ **P4 已補齊**：GAS `transferOut`（`TRANSFERRED_OUT`＋`transferTo`／`transferDate`）／`importTransferBundle`（sha256 → transferId 冪等 → 撞號阻擋 → 建 `pending_hash`）／家長（同旅零改動；轉旅＝來源家長戶 `LEFT`＋通知文案俾人 copy）；前端 `lib/transfer.js` 同一套規則（示範模式都行同一套閘）＋`lib/hash.js`（canonical 欄序同 GAS 對齊、WebCrypto sha256，冇 crypto 就明確標示示範雜湊）＋UI 分「移出（來源團）／接收（目標團）／升團季批量／歷史」；教材 10 | **~90%**（未同真團對打） |
| **同步與多人寫入** | staging 黃點、版本樂觀鎖、merge3、ScriptLock 排隊、tombstone purge | ✅ 前端 `offline.js`（算法）＋**`sync.js`（司機，P3 接落 UI）**：三色燈＋樂觀鎖 base／baseVersion＋**merge3 逐行逐欄合併**（唔同欄各自保留／同一格衝突**逐格問「用我／用佢」，唔會自動揀**／無人看場＝serverTime 新者勝＋`overwrote` 留底）＋**backoff＋jitter 硬化（P8）**：錯誤碼統一（`codeForStatus`：429／5xx／斷網 → 可重試 code，權限／規則／撞版唔會盲重試）＋**讀取都會重試**（之前只有寫入）＋隊列逐筆記 `tries`／`nextAt`（未夠鐘唔敲後端、人手撳＝即刻試、開機／回前景夠鐘自動補送、防重入）＋UI 顯示「下次自動重試幾秒後」＋離線隊列（≤200 筆，送唔到唔會跌）；UI＝系統 →「同步」（燈號卡／立即同步／無人看場／逐格判斷對話框／隊列重試）；後端：`ScriptLock` 20 秒、逐表寫＋讀返自證、大庫分件、tombstone＋90 日 purge、**版本樂觀鎖**（`getVersion`／`loadTables` 回 server 版本；寫入帶 `baseVersion`，對唔上＝`conflict` ＋回現行版本，前端自動重讀重合併一次，**唔會覆蓋人哋改動**） | **~95%**（未同真後端對打） |
| **備份** | `exportAll` 單一 JSON＋sha256、每週 Drive 留 13 份、`importAll` 冪等、三時機提醒 | ✅ GAS `exportAll`（帶 sha256 指紋）／`importAll`（保留舊 hash、冪等、≤2000 筆）／`backupToDrive`（建立即 PRIVATE）／`備份紀錄` 分頁；✅ **P2b**：13 份輪替（`backupFileName_` 自家格式先刪、13 份以外 `setTrashed`）＋`backupState` 三時機提醒（升級前／批量前／每週）＋UI 備份卡（即刻做／落本機／匯入前警告、示範＝本機紀錄）；⏳ 未上真 Drive 實測 | **~85%** |
| **體積治理** | 零依賴 `api`、`.vercelignore`、`dist<5MB`、AVIF、CI 硬攔 | ✅ 零依賴 `api`、`.vercelignore`、`vercel.json`（hkg1）；✅ 新增 `formats.js`：上載 ≤5MB／每筆 ≤3 張／每日上限／AVIF·WebP 優先＋dist<5MB·bundle<2MB 檢查（`npm run check` 就係嗰條硬線）；⏳ 未接真 CI workflow | **~85%** | ＋ **文件數字守衛**（smoke：README／UI 導覽／落差報告嘅測試數字、檔數、體積 KB 要同真測試／lint 對得上；舊數字要標「當時」）

**整體（2026-09-26 · P15 更新）**：以「功能齊備＋規格對齊」計 **~99%**；以「真用得（後端＋接駁）」計 **~77%**
（餘下：ADMIN 人手部署／驗收、各下游 portal 抄範本、館方推送鏈開通 —— 全部本 repo 之外）
（後端兩層寫好＋本機測試綠；**未上真 Sheet／真 Vercel／未同真下游對打** —— 呢三樣係剩下嘅主要距離）。

---

## 2. 大項未做（排序＝BUILD §10 施工次序）

> 2026-09-26 更新：**問題回報去 ADMIN 收件匣**已對正合同（見 §3 #10）—— `/api/proxy` 落 `action:'issue'` 就即刻通。

### ① 旅 `Code.gs`（P0 後端）—— ✅ 已寫好（2026-09-26），剩「上真 Sheet 實測」

> `apps-script/Code.gs`（833 行）＋ `scripts/gas-test.mjs`（假 Apps Script 環境 · 23 項）。詳見下方原本清單（全部已實作）。
> **下一步（人手）**：貼上 GAS → 跑 `initializeSheets()` → 選單「🔑 顯示 BACKEND／APIKEY」→ 放入 Vercel env → 用真 Sheet 行一次「測試連線」。
- `doGet`/`doPost` 首行 `requireAuth`；**key 未設定＝拒絕敏感 action**（fail closed）
- **`sig` 驗簽**：`HMAC-SHA256(action+ts+nonce+sha256(body))`、±5 分鐘、nonce 一次性（CacheService 10 分鐘）、雙通道（query 防 GAS 302／body）
- **action 白名單**（讀 12／寫 19）＋永不接受清單；`setGate`／`upsertUser`／`bulkAddUsers`／`exportAll`…
- **ScriptLock `waitLock(20s)`**、原子寫（暫存行＋swap）、逐表寫自證（寫完讀返）
- `AUDIT_LOG`（`prev_hash` 鏈）／`ACCESS_LOG`（append-only、server-side）
- `dbInfo`／`status`（`backendVersion`）、`exportAll`／`importAll`、Drive 備份＋每週排程
- 生成物唔 commit：`scripts/build-gas.mjs` 產生 `apps-script/Code.gs`

### ② `/api/*`（Vercel functions，零依賴）—— ✅ **九支已寫好**（2026-09-26 · P1 完成）

> `proxy`（`issue` ＋ 旅 GAS action 白名單 ＋ **旅長／教練員分權**）、`auth`（PBKDF2 100k／session）、`super`（票據）、
> `units`、`downstreams`、**`troop`（旅層聚合：分層 cache 5／30 分鐘＋`?fresh=1`）**、
> **`registry`（能力表：模組開關＋支部燈號＋`branch=` 逐支部答「用唔用得」）**、
> **`member-entry`（M1／M3 導流；唔做帳號枚舉）**、**`share`（簽名連結：通告／活動、最長 90 日、過期自動失效）**。
> 本機 27 項測試綠。
- `/api/proxy`：驗 session → 注入 apikey → 打旅 `/exec`
- `/api/auth`（登入／登出／改密碼）、`/api/super`（**固定受信端點回打驗票**：60 秒票據、AES-GCM、一次性防重放）
- `/api/units`、`/api/downstreams`、cache 5 分鐘＋flush endpoint
- Session：JWT HttpOnly `Secure; SameSite=Lax`、≤30 分鐘＋silent refresh

### ③ 真密碼與安全
- PBKDF2-SHA256 ≥100k＋per-user salt＋timing-safe compare；舊 hash 首登自動升級
- 每帳號 5 次失敗鎖 15 分鐘；`mustChangePw` 期間只放行改密碼 API；改密碼 `pv+1`
- ✅ **（P15）上線就緒：`npm run preflight` ＋ 部署／驗收文件更新至 P14**：`scripts/preflight.mjs`
  —— ① 環境變數（必要／可選、逐個旅兩件齊唔齊；**只報變數名同長度，永遠唔印值**，有測試釘死）
  ② 靜態就緒（關鍵檔在唔在、`units.json` 冇 key／URL、`api` 零依賴、部署體積 <5MB）
  ③ 交返「跟住做」清單；未齊必要 env ⇒ exit 1（唔會扮綠）。`docs/後端部署步驟.md` 更新：
  新 env（`APP_URL`／`VAPID_PUBLIC_KEY`／`PUSH_INGEST_URL`／`PUSH_INGEST_KEY`）、下游範本步驟、
  **驗收清單加 10 項**（忘記密碼一次性連結、子女綁定確認、樂觀鎖 conflict、讀取覆查、sig jti 重放、
  log metadata、移交四道閘、推送訂閱、下游範本、preflight）、出事表加 4 行
- ✅ **（P14）個人化訂閱推送鏈（前端＋收件閘）**：見上表；另外**CI 硬攔**（`.github/workflows/check.yml`：
  lint／GAS／api／smoke ＋ 部署體積 <5MB 檢查）
- ✅ **（P13）db shard 硬化（大庫分件）**：① 件數上限 `LIMITS.maxParts=40` —— 超出＝**誠實拒**，
  「今次一個字都冇寫」（唔會寫一半）；② 分件寫入**前後各 bump 版本一次**（write-ahead ＋ write-behind）
  ⇒ P11 嘅 pointer 覆查一定偵測到「寫緊」，讀者唔會讀到一半新舊混合（修好 P11 遺留嘅一個真漏洞：
  之前版本係寫完先 bump，分件寫入中途嘅讀取會驗成「一致」）；③ 任何一件寫唔入＝**回滾**返寫入前嘅樣
  （`restoreShard_`；回滾都失敗會落 `操作紀錄` 叫即刻由備份匯入）。`dbInfo` 報分件現況
  （`shard.sharded`／`shards`／`largest`／`unbalanced`）；系統 →「後端」加「真後端檢查（status ＋ dbInfo）」
  掣（示範模式零 fetch、老實講「唔會假裝連到」）
- ✅ **（P12）leaf 自製 session token ＋ 下游接入範本**：`apps-script/Downstream.gs` ——
  下游驗完上游 sig 之後**自己簽**短期 token（`base64url(payload).hex(hmac)`，密鑰＝本機 `API_KEY` 派生，
  **同上游 sig 密鑰分開**），之後下游自己嘅請求只帶呢張 token ⇒ **唔使每 request 驗 sig、亦唔使回打上游**
  （三回調鐵律：下行簽名單向、下游永不回打）。失效四路：到期／`pv` 變（改密碼即全體失效）／`node` 唔對／換 key。
  同時交齊閂口（`ALLOW_LOCAL_LOGIN`＋403 求救指引）同 `never_accept`（login／apply／改密碼永不經簽名接受）
- ✅ **（P11）讀取樂觀化（pointer 覆查，唔用全 read lock）**：GAS 加讀取白名單
  `READ_ACTIONS = [status, dbInfo, load, loadTables, getVersion]` —— 讀取**唔攞全域寫鎖**（之前連讀都要等 20 秒鎖，
  一 busy 就誠實拒）；改為 pointer 覆查：讀 version → 讀表 → 再讀 version，兩次一樣＝同一版；
  唔一樣就重試（最多 3 轉），仍然唔穩就回 `consistent:false` ＋ note（**唔會扮一致**）。
  前端：`sync.js` 收到唔一致會記 `lastReadNote`（成功都留低，唔當錯誤）；系統 →「同步」加「上次讀取覆查」一行。
  寫入照舊攞鎖 ＋ 樂觀鎖（讀者唔上鎖唔影響正確性：撞版一定 conflict）
- ✅ **（P10）sig jti（一次性簽名）**：`nonce` 就係 jti，但「cache 快路 ＋ **持久環**」雙保險 ——
  `sn_<nonce>`（CacheService 10 分鐘）＋ `SIG_JTI_RING`（ScriptProperties，上限 200、
  過 ±2×skew 自動淘汰、只存 24 位 hex 摘要唔存 raw nonce）。cache 蒸發／到期都照樣擋得住重放；
  重放回穩定 `sig_replayed` code ＋ audit 記低 `jti` 前 8 位（metadata only）。
  簽名 canonical 唔變 ⇒ 現有下游（vsbadge 等）零改動
- ✅ **（P9）紀錄只記 metadata**：全部 log（AUDIT_LOG／ACCESS_LOG）經同一支 `redactMeta_` 收口 ——
  長文字（通告內文、退問原因、求救詳情…）唔入 log，只記 `[內容不記錄 len=N]`；
  email 只留頭 2 字＋網域；香港電話只留尾 4 位；短 id／狀態／版本照樣留住（追蹤要用）。
  遮完先算 hash，所以 `prev_hash` 鏈照樣驗得到；前端本機審計用同一套規則（`redactMeta`），
  審計卡寫明「只記 metadata」
- ✅ **P6 忘記密碼**：`/api/auth action=forgot`（限流 3 次／10 分鐘／IP）→ GAS `issueResetToken`
  種 12 字 token（30 分鐘）＋`MailApp` 寄一次性連結（未設 `APP_URL` 或寄唔到＝老實交 token 俾旅長人手傳，
  **唔會扮寄咗**）；**防帳號枚舉**（搵唔到 email 都回同一個形狀）；設密碼行 `setupWithToken`（server 側 PBKDF2，
  GAS 永遠唔見明文）；UI：登入頁「唔記得密碼？」＋`?step=setup` 設定頁（連結帶 token／email 自動填好）
- 邀請連結（12 字元、24 小時）
- 秘密掃描測試（掃死後門／舊 hash）

### ③b 已補（P1 · 2026-09-26）

- ✅ **大庫分件**：`saveTable`／`saveTables` 超 20,000 行自動切件（`旅通告#1`…），`load`／`loadTables` 自動合返；縮返細會刪走分件
- ✅ **`saveDbPart`**：前端主動分件寫（part 0＝主分頁＋`reset`）
- ✅ **tombstone**：`deleteRow` 寫墓碑（邊個／幾時／為咩）＋`getTombstones`；`purgeTombstones` 90 日清理
- ✅ **權限封頂**：`updateUserRole`／`updatePermissions` 唔可以授唔低過自己嘅角色；權限只可以授自己角色有嘅，超出＝**如實回報 `clamped`**
- ✅ **匿名寫入限流**：求救（免登入）每 IP 每個鐘 ≤3 單；落 `操作紀錄`
- ✅ **前端**：`offline.js`（三色燈／樂觀鎖／backoff＋jitter／離線隊列）、`push.js`（匿名訂閱）、`formats.js`（上載＋體積）
- ✅ **UI**：通告詳情「簽名分享連結」、行事曆「訂閱日曆／活動分享」、求救真模式落旅 SHEET

### ④ 真 `sig` 接駁
- 旅側落碼後，用 **`vsbadge`／`roverbadge`（已 built）** 做首個實測：登記 → 測試連線 → 開戶 → 搬數 → 閂口
- 其他團照 §4.8 清單補（`handleSignedRequest`／`DOWNSTREAM_*`／`ALLOW_LOCAL_LOGIN`／`SUPER_VERIFY_URL`／守護測試）
- **閂口次序鐵律**（升級 MD §12④）：**先搬數、先測連線，先至閂口**；而且「未起好進度 sig 路徑之前唔好閂口」

### ⑤ 同步與多人寫入
- 版本由 server 派（ISO＋隨機尾數）、寫入帶 `baseVersion` 樂觀鎖、撞版回 `conflict:true`
- merge3 欄位級合併（同格衝突逐格確認）；ScriptLock busy → 前端排隊重試（backoff＋jitter）
- 大庫分件（`saveDbPart`）＋分段讀；刪除＝tombstone，後端定期 purge

### ⑥ 個人化訂閱 ★（BUILD 寫「重中之重」）
- 前端訂閱設定 → **圖書館現有推送鏈**（Supabase `push_subscriptions` → GitHub Actions 06:00 `notify.py` → pywebpush）
- 要圖書館側改：`require_same_origin` 加 origin allowlist；`push_subscriptions` 加 `source` 欄
- 通告頁＝本單位通告＋已訂閱圖書館通告（同頁同列表、來源標示）

### ⑦ 平台與教材
- ADMIN 收件匣 GAS（只服務獨立前端接入、per-IP 限流、2FA）＋ units.json／Vercel env 流程
- 教材三層（角色快速入門／模組說明／MOCK 引導任務）＋「開旅 checklist」

### ⑦b 匿名可寫面（BUILD §3）—— ✅ 已全套落碼（2026-09-26）

> 每種都係「**白名單 action ＋ 限流 ＋ 寫入待批表**」：
> 通告報名（同通告同名去重）／物資借用（同人同件同日）／收支申報（同支部同期）／進度申報（同支部同期）／
> 開戶申請（**同 YMIS 待批唯一**）／求救（免登入，每 IP 每鐘 3 單；申請類 10 單，**分開計**）。
> 領袖決定：`decideApplication`（批／拒；**拒一定要寫原因**）；批開戶 ＝ **即回一次性邀請 token**；
> 純邀請制 `setApplyMode`（求救唔受影響）。測試：GAS 34 項內含 3 組匿名面／去重／批核。

### ⑨ 教材三層（BUILD §9）—— ✅ 已落 `docs/教材/`（跟版本走）

> 角色快速入門 ×5（旅長／教練員／家長／支部人員／平台超管）＋模組說明＋MOCK 引導任務＋開旅 checklist（A–F）＋開戶與批核；
> UI「系統 → 教學」有同一份對照表（`DOC_FILES`），smoke 會驗檔案真係存在（唔可以有死連結）。

### ⑧ 未做嘅功能模組（UI 已有目錄／部分）
- **相簿**：上載（單檔 ≤5MB、每筆 3 張、每日上限）、公開發佈要家長同意確認 —— 用戶已定案**今輪唔開**
- 進度頁（B 模式打進度 `/exec`）—— 屬支部系統；旅側只留接入位
- 財務／物資「真提交」（支部用自己 key 簽）—— 目前旅側有 UI 同示範流程

---

## 3. 同 BUILD 對唔上／要你拍板嘅位（你話「我知道有些地方要調整」）

| # | BUILD／升級 MD 寫法 | 我哋而家 | 要點 |
|---|---|---|---|
| 1 | **公開頁要登入先入到**（＋公開帳共享密碼，冇戶外人入唔到） | `public.html` **免登入**（旅公開資料 lvl0） | ✅ **定案（2026-09-25）：維持免登入** —— 用戶：「你而家果個都 OK」。唔跟 BUILD「要登入＋公開帳」；BUILD 文件加註 |
| 2 | **每個 leaf 至少留一本地領袖戶＋SUPER 作災難恢復** | 求救制；**本地領袖戶／SUPER 本地戶一樣 403** | ✅ **定案（2026-09-25）：指 DELETE ACCOUNT 要保留最小 1 個** —— 唔係要保留本地登入通道。已落碼：`removalGuard()` 擋「刪／停用最後一個領袖戶」；`removeUserAccount()` 留紀錄；超管（隱藏）刪唔到 |
| 3 | **閂口次序**：先搬數、先測連線，先至閂口；「未起好 sig 路徑之前唔好閂口」 | 閂閘只需確認（有求救提示） | ✅ **定案＋已落碼（2026-09-25）：加前置檢查**（接駁燈綠／測試連線成功／進度下游已登記＋帳號下限提示）；未達標**出警告、唔硬擋**；審計記低前置結果（`gatePreflight()`／`applyGate()`） |
| 4 | **內部分享＝發佈時揀支部** | 收件方決定（接收先出現） | ✅ **定案＋已落碼（2026-09-25）：兩者並存** —— 用戶：「係我發你可以唔收，我想深資活動想童軍支部參與姐，我童軍團領袖唔想比仔參與唔得咩?」發起方一次揀（多選）對象，每個收件支部獨立收／退；維持「接收先出現」。`readme` 加註：**我冇 push 權限**，文字已備（建構計劃附錄 D），要你／ADMIN 貼 |
| 5 | **`TROOP_OPS` 表唔建**（Registry 只落 ScriptProperties） | 一致 ✅ | 冇落差，記錄備查 |
| 6 | **零回打版**（cubsbadge／scoutbadge）中央登入用 Vercel 驗 `SUPER_KEY` | 示範資料已標「（零回打版）」 | 對齊時改跟 ① 回打 `/api/super` |
| 7 | **成員登入**：BUILD 三點進入（sig／本地／SUPER） | UI 行 M3（旅只帶你去該團入口） | ✅ **定案（2026-09-26）：混合** —— M3 做底；已登記＋綠燈＋測過連線嘅團自動 M1（一次登入，旅側經 sig 交該團驗）；密碼 hash 住該團 SHEET。已落碼：`loginRouteFor()`＋支部登入頁顯示 |
| 8 | **進度頁屬支部系統**（B 模式） | 旅側有「進度摘要」tab（純示範） | 接入要等團側；旅側唔做進度 UI（BUILD 豁免）✅ |
| 9 | **`permissions_override` 封頂**（下級 ⊆ 上級） | 逐人 `perms`（rank／借用／公開上報）已做，未做封頂驗證 | 後端落碼時一齊做 |
| 10 | **問題回報去 ADMIN 收件匣**（`BUILD` §7 ADMIN APP 收件匣） | 舊寫法係 `kind:'issue'` ＋ `message` ⇒ 同 scout-admin「問題回報 TICK」**格式唔對**（落到 ADMIN 係一堆冇名欄位，同圖書館 report.html 唔通同一張表） | ✅ **已對正（2026-09-26）**：`type:'issue'` ＋ `sourceApp:'troop_portal'` ＋ `title/desc/severity/troopId/name/contact`；嚴重度白名單（亂填落「高」）；經 `/api/proxy` 由 server 側送；**ADMIN 側零改動** |

---

## 4. 等你答（唔答我就照「建議」落碼）

> **2026-09-25 進度**：第 3 節表嘅 #1–#4 已定案並落碼（見 ✅ 行）；下面 §13 六條仍未答。

**另加（2026-09-26 用戶指正）**：**問題回報 TICK 合約已對正**（第 3 節表 #10）—— 求救表＝標題＋嚴重度＋問題詳情，格式同 scout-admin `report.html` 完全一樣；ADMIN 收件匣唔使改任何嘢；示範模式只入本機＋審計（前端零 fetch）。

**§13 六條 —— ✅ 全部已定案（2026-09-26 用戶答）**（決策過程：`docs/13-決策簡報.md`）：
1. 成員登入路線：**混合**（M3 做底；接駁好嘅團自動 M1 一次登入）＋密碼 hash 住該團 SHEET
2. 旅 ID 格式：**4 位補零**（`normId` 單一實現）
3. ✅ **（P7）家長綁定子女：該團領袖確認先睇到** —— GAS `bindChild`／`decideBind`：
   申請要對名冊（名冊冇＝即時擋 `no_child`）、領袖確認先寫落家長戶 `children`、
   唔會動子女自己嗰個戶、拒一定要原因、同一張唔可以決定兩次（防重放）、重複申請自動去重；
   UI：家長「我的子女」申請（真模式打 API）／待辦「子女綁定」分頁確認，
   名冊對唔上直接標 ⚠️；smoke＋gas 各有專門測試
4. 整合數據新鮮度：**分層 cache**（通告／活動／點名 5 分鐘；財務／物資／進度 30 分鐘）＋強制刷新
5. `TROOP_MODULES` 存邊：**旅 SHEET 分頁＝真相＋5 分鐘快取**（只限旅長改；閂咗＝隱藏唔刪）
6. `readme` PR：**列 backlog**（零回打版例外先記住；P0 之後一次過拉齊）

> 六條之中，UI／示範層做得到嘅（登入路線顯示、分層 cache＋強制刷新、模組開關說明、normId 驗證）**已落碼**；
> 真後端部分（`sig` 派票、cache 實作、分頁寫入）等 `Code.gs` P0。

**第 3 節表嘅三條（公開頁／災難恢復／前置檢查／分享）已於 2026-09-25 定案，見上表 ✅ 行；落碼已完成（當時快照：lint 35 檔 · 697 KB ／ smoke 60/60 —— 歷史數字，現況見 §0／§1）。**

---

## 5. 次序（P0–P15 · 全部完成）✅

1. ~~§13 六條定案~~ ✅（2026-09-26；落碼見下）
2. ~~旅 `Code.gs` P0~~ ✅ **P0（fcc73c5）**＋P1／P6／P9–P13 硬化（本機 **53 項**綠）
3. ~~`/api/*`~~ ✅ **P1（214efe6）**：`proxy`／`auth`／`super`／`units`／`downstreams`／`troop`／`registry`／`member-entry`／`share`（＋**P14 `push`**）＝**10 支**，本機 **27 項**綠
4. ~~真 `sig` 實測~~ ✅ 本 repo 側齊：`Downstream.gs` 範本＋教材 12；⏳ 各團複製範本（本 repo 唔代人改其他 repo）
5. ~~個人化訂閱（★）＋ Drive 備份~~ ✅ **P14（53cf6f1）**：前端四步＋`/api/push` 收件閘＋`sw.js`＋教材 13；Drive 備份 13 份輪替＋三時機提醒（P2b）
6. ~~平台收件匣（另一個 repo）＋教材三層~~ ✅ 教材 1–13＋`DOC_FILES` 對照（smoke 驗檔案存在）；ADMIN 收件匣屬 Scout Admin repo（本 repo 側合約已對正：`type:'issue'`）
7. ~~上線就緒~~ ✅ **P15（939612d）**：`npm run preflight`（env 只報名／長度 + 靜態就緒 + 體積）＋部署步驟更新到 P14（驗收清單 22 項）

## 6. 收尾後仲有咩（全部喺本 repo 之外）

| # | 事項 | 邊個做 | 參照 |
|---|---|---|---|
| 1 | 貼 `Code.gs` 上真 Sheet → `initializeSheets()` → 攞 BACKEND／APIKEY 入 Vercel env | ADMIN 人手 | 部署步驟 §1–§3 |
| 2 | Vercel env：`SESSION_SECRET`／`TROOP_<id>_*`／`SUPER_KEY`／`APP_URL`（＋推送三個 env） | ADMIN 人手 | 部署步驟 §2（先跑 `npm run preflight`） |
| 3 | 真環境驗收（22 項：基本＋P6–P14） | ADMIN 人手 | 部署步驟 §5 |
| 4 | 各下游 portal（vs／cubs／scout／gh）複製 `apps-script/Downstream.gs` 範本＋登記連線 | 各支部／ADMIN | 教材 12 |
| 5 | 館方開通推送鏈（Supabase `push_subscriptions` ＋ VAPID ＋ 06:00 `notify.py`） | 圖書館側 | 教材 13 |
| 6 | 相簿／財務物資真提交／進度頁 —— 用戶已定案今輪唔開（UI 目錄保留） | — | 落差報告 §2⑧ |
| 7 | `readme`（真理倉）加註文字貼上 | 用戶／ADMIN（我冇 push 權限） | `docs/readme-加註文字.md` |

> 之後每次落碼照舊：先改本計劃（同呢份落差報告），再落碼；`npm run check` 要綠。
