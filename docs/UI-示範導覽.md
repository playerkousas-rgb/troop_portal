# 旅系統 UI 示範導覽（功能齊備度檢查用）

> 目的：唔使睇碼、唔使等後端，都可以逐項檢查「功能係咪齊備」。
> 呢一版係 **UI 先行示範**：純靜態、零依賴、全部資料係示範旅 MOCK（住你部機嘅 localStorage），
> **唔會連任何後端**，亦**唔會**假裝寫入成功。

- 版本：`v0.1.0-ui`（示範模式）
- 對應計劃書：`docs/旅系統建構計劃.md`
- 真後端（旅 GAS ＋ /api/proxy）**未實作**；UI 上所有「真模式會點」都寫明係示範。

---

## 0. 點開

```bash
npm run dev          # → http://localhost:8080/
# 或者
python3 -m http.server 8000     # 任何靜態伺服器都可以（零 build、零依賴）
npm run check        # lint（語法／匯入圖／註冊表／CSS class／體積）＋ jsdom smoke（32 個場景：4 個角色 × 全部路由、13 模組 × 全部分頁、355 個掣逐個撳）
```

**示範帳號（密碼一律 `demo1234`）** —— 按旅閘嘅**身份分流**入：

| 身份（旅閘揀） | 帳號 | 睇得到咩 |
|---|---|---|
| 旅長／教練員 | `chief@demo.troop`（旅長）／`coach@demo.troop`（教練員） | 旅長＝15 個模組全部；教練員＝除「系統」以外，只睇 branch_access 嘅支部 |
| 家長（監護人） | `parent@demo.troop` | 我的子女（跨 2 個支部）、通告、行事曆、支部、公開資料 |
| 支部人員（**要先揀團**） | 幼童軍團：`cs-leader@demo.troop`（團長）／`cs-deputy@demo.troop`（副團長）；深資童軍團：`vs-exec@demo.troop`（執委 · 主席）／`vs-team@demo.troop`（團隊長 · 18+）；童軍團：`sc-cpl@demo.troop`（副隊長 · 未夠 18）／`sc-scout@demo.troop`（團員 · 18+，試「唔夠權決定分享」） | 我的支部（身份卡）、通告、行事曆、物資、**分享中心**；團長／副團長多一個「支部」入口 |
| 平台超管（**隱藏**） | `super@platform.local` —— 入口：`index.html?step=super` 或旅閘撳 ⚜ 五下 | **全部模組都入得**（唔經支部 SHEET 登記／接駁：未登記／紅燈／閂咗閘／接駁斷都擋佢唔住）＋平台（接入收件匣、units、金鑰輪換、「★ 超管救援」重設 ADMIN 密碼）；唔會出現喺任何名單 |

**★ 支部人員入唔到小童軍團**：示範旅嘅 `gs0082` 係「紅燈＝未登記下游」，揀團頁會紅字講明，撳「點解入唔到？」有解釋
—— 因為旅讀唔到該團資料，就對應唔到支部系統嘅後端（誠實失敗，唔會扮成功）。

**★ 旅入口＝你支部入口（2026-09-25 你定案）**：揀啱團、登入之後**就直接係你支部嘅世界**（名冊、自己團通告／活動／物資），
唔會再叫你開第二個系統、亦唔會再登多次。支部系統本身嘅介面保留（你選「支部保留自己介面」）——
旅負責門戶、身份、聚合同分享。你睇到嘅嘢 ＝ **你支部嘅嘢** ＋ **其他支部 share 咗並經你哋接收**嘅嘢。

**★ 分享只有兩樣（通告 ／ 活動）**：接收咗之後只會出現喺**通告頁**（一組「來自 XX 團」）同**行事曆**
（月曆格用斜紋「分享」樣式 ＋ 本月活動表）。物資／進度／相簿等種類 UI 唔開，之後先加。

**★ 求救／問題回報（2026-09-26 對正 ADMIN 合同）**：`index.html?step=rescue`（免登入）＝
**標題 ＋ 嚴重度（低／中／高／緊急，默認中）＋ 問題詳情（≤2000 字）**，另加支部（`troopId`）／你係邊個／聯絡方式。
送出＝**兩份**：① 旅系統求救單（ADMIN 人手處理：開返閘／重設密碼／答覆）② **Scout Admin「問題回報 TICK」**
（`type:'issue'`、`sourceApp:'troop_portal'`，同圖書館 report.html 同一支 GAS、同一張表）。
ADMIN 側**唔使改任何嘢**；後端未接駁時老實講，並提供官方回報頁（同一個收件匣）。
（本機 `dev-server` 收同一條 `/api/proxy` 路徑但只做 **sink**：log 出 payload、**唔會真喺 ADMIN 開 TICK**；真送＝Vercel 上 `api/proxy.js`。）

**★ 支部系統登入通道（開／閂）**：就係一件事 —— **旅決定閂唔閂「由支部系統自己登入」嗰條通道**。
閂咗：就算支部系統已登記、經 sig 睇到張 SHEET，都**唔畀佢自己登入**（403）—— 出入只經旅入口；
**旅入口一律照入得**。控制面喺**旅側**（支部 → 接駁與登記；支部系統嗰邊冇掣），撳一下 → `sig` 打 `setGate` → 下游回 `confirmed`。
**兩邊都開（open）都完全得** —— 純粹係旅嘅決定，冇「一定要閂」嘅標準狀態。示範：童軍團＝兩條通道都開；其餘四個支部＝閂咗支部系統登入。

**★ 支部版面：旅側唔另設**（各支部自己設計，各支部唔同）。你而家見到嘅係**通用版面**，只為睇功能齊備；
之後把各支部設計好嘅版面**照抄**入嚟就得（`#/mine` 有「支部版面」卡，寫明接入方式同狀態）；
換版面**唔會**影響身份／職稱、權限、分享、接駁、審計。

**★ 分享：發方揀對象、收方決定（兩者並存，2026-09-25 定案）**：發出時**可以揀多過一個支部**
（或全旅）；**每個收件支部各自決定** —— 接收先會出現喺佢哋清單，退回就留紀錄（邊個決定、幾時、理由）。
未接收＝淨係喺「分享中心 · 待接收」見到，**唔會硬塞**。
用戶原話：「係我發你可以唔收 —— 我想深資活動想童軍支部參與姐，我童軍團領袖唔想比仔參與唔得咩？」

**★ 刪除帳號要保留最小 1 個（2026-09-25 定案）**：名單每個帳號都有「刪除」，但**刪／停用之前會過下限守衛** ——
每個 leaf（旅 SHEET／每團）**至少留 1 個領袖戶**；刪淨最後一個會被擋（誠實失敗，唔會扮成功）。
超管（隱藏）唔可以刪／停用（第二層備援）。刪除會入「已移除」紀錄（邊個、幾時、邊個做）。
（留意：呢條同「閂咗支部系統登入，本地戶一樣 403」冇衝突 —— 一個係「有冇戶口」，一個係「本地登入通唔通」。）

**★ 登入路線：混合制（2026-09-26 定案）**：支部登入頁會標明行邊條路線 ——
**「一次登入（M1）」**（已接駁＋綠燈＋測過連線嘅團：喺旅閘打一次密碼，旅側即刻經 sig 交該團驗，旅唔存密碼）
／**「轉去該團入口（M3）」**（未接駁：旅側只做門戶，密碼由該團自己驗）。
示範：幼童軍團係「一次登入」；童軍團（未測過連線）係「轉去該團入口」。**改好一個團升一個團，團側零改動都照跑得。**

**★ 資料新鮮度：分層 cache（2026-09-26 定案）**：通告／活動／點名**最多 5 分鐘**；
財務／物資／進度**最多 30 分鐘**；每頁有「**強制刷新**」掣（清 cache 即時向下游拉，會入審計）。
（示範模式唔連接後端，撳掣只係示範流程＋入審計。）

**★ 模組開關（2026-09-26 定案）**：真相住**旅 SHEET「模組開關」分頁**（改一次寫一次＋入 AUDIT，跟備份走），
只做 5 分鐘快取；改得嘅只有**旅長**；**閂咗模組＝隱藏，唔會刪資料**。睇：旅長 → 系統 → 模組開關。

**★ 閂口前置檢查（2026-09-25 定案）**：撳「閂支部系統登入」之前，系統自動列 4 項：
接駁燈綠／測試連線成功／進度下游已登記／帳號下限提示。**未達標出警告，但唔硬擋**（你仍然可以閂），
審計會記低前置結果。
未夠決定權（團員／隊長）睇得到，可以**加註解**交團長／執委 —— 但唔會當你決定咗。

**角色模型（2026-09-25 你定案）**：旅層只有 **旅長** 同 **教練員**（冇「旅層領袖」）；其餘全部係支部人員
—— 身份：團長／副團長／管委／執委／隊長／副隊長／團隊長／團員；職稱：主席／副主席／秘書／財務（默認跟執委或管委，可按人微調）；
年齡組：**18+ ／ 未夠 18**（由生日自動計；未夠 18 要監護人＋家長同意）。

---

## 1. 一分鐘路線（想快睇全貌）

1. 開 `index.html` → 旅閘（揀旅、伺服器登記狀態、診斷、新旅部署申請）
2. 撳「入第八十二旅」→ 揀身份（旅長／教練員）→ 登入（示範帳號一撳即入）
3. 儀表板（細 chips ＋ **全部區塊默認收合**，撳一下先展開）→ 左邊導航逐個撳（分組可收合，順序＝分期 P0 → P3）
4. 尾二個掣：頂部 **「儲存到後端」**（唯一寫入掣）、右上角 **帳號**（改密碼／登出）
5. 睇 **我的支部**（#/mine）＝身份卡／權限／年齡組；最後撳導航底部 **公開頁**（`public.html`）睇免登入視野
6. **試「收件方決定」**：用 `sc-cpl@demo.troop`（童軍團）登入 → 儀表板已經見「需要你（你支部）決定 3 項」→
   分享中心撳「接收」→ 返通告頁，即刻見到「來自 深資童軍團」；再試「退回」要填理由（會留紀錄）。
   想試「唔夠權」：用 `sc-scout@demo.troop`（團員 18+）→ 收件方決定嘅掣唔見咗，只有「加註解交團長／執委」。
7. 想睇超管：登出 → 旅閘撳 ⚜ 五下（或 `?step=super`）→ `super@platform.local` / `demo1234`

---

## 2. 逐個模組檢查（每個都寫明可以試咩）

| 模組 | 路線 | 可以試啲咩（示範會真係改本機資料） |
|---|---|---|
| 分享中心 ★ | `#/shares` | 待接收（接收＝先出現／退回＝留理由）／已接收（可收回）／我哋發出（可撤回）／分享規矩；**種類只有通告＋活動**；未夠權者只可以加註解 |
| 我的支部 | `#/mine` | 身份卡（身份／職稱／年齡組／可見等級）、監護人與家長同意（未夠 18）、本團物資、通告、分享中心入口；旅長／教練員版本＝授權支部一覽 |
| 儀表板 | `#/dashboard` | 一行 chips（支部／接駁／現役／待辦／淨額／未寫入）＋ **全部區塊默認收合**；還原示範資料 |
| 待辦與批核 | `#/pending` | 統一待批：帳號申請、跨團幫手、財務提交、公開上報、移交接收；批准／拒絕要填原因（入審計） |
| 支部 | `#/branches` → 撳入任一支部 | 8 個分頁；「進入支部」三路（P 門戶／S 簽名／A apikey）、測試連線、為下游開戶、批量搬數、問旅長 |
| 通告 | `#/notices` | 發佈／草稿、分享俾指定支部（分享前設＝接收方有模組）、QR、報名名單、**我嘅訂閱 ★**（支部 × 分類） |
| 行事曆 | `#/calendar` | 月曆 ＋ 本月活動；每個支部一色可勾選；加活動（要 `calendar_edit`）；匯出 ICS。★ 已接收嘅**活動分享**用斜紋「分享」樣式落喺月曆格，本月活動表標「來自 XX 團」 |
| 財務整合 | `#/finance` | 整合總覽／逐支部／旅帳／報表；確認／**退問**（支部改完再提交）；匯出 CSV |
| 物資整合 | `#/inventory` | 清單／借用與歸還／共享設定；借用路由去 **owner**；旅部物資即時通過；拒絕／歸還／加減庫存 |
| 移交與升降團 | `#/transfers` | 待接收、**升團季**批量、查看移交套裝 JSON（sha256）、接收＝建 ACTIVE、`transferId` 冪等 |
| 用戶與身份 | `#/users` | 帳號名單（超管唔會出現）、**身份與職稱**（團長…團員／主席…財務 ＋ 逐人微調）、發邀請（教練員／家長／支部人員）、`branch_access`、權限總表、停用／復原（旅長鎖死） |
| 公開資料 | `#/public` | 旅公開資料（改可見等級 0–5）、支部上報待批、分享連結／QR／海報、**遊客視角預覽** |
| 我的子女（家長） | `#/children` | 子女跨支部自動併埋：進度摘要、通告、活動履歷、繳費狀態；新增子女＝送該團領袖確認 |
| 平台（超管 · 隱藏） | `#/platform` | 接入收件匣（批新旅／新下游）、units registry（`data/units.json` ＋ Vercel env）、金鑰輪換與外洩應變、**★ 超管救援**（重設旅長／ADMIN 密碼 ＋ 睇求救單 ＋ 一鍵入任何支部） |
| 系統（旅長） | `#/system` | 8 個分頁：旅團設定、**模組開關**、後端實況（逐表寫自證）、審計與操作紀錄、自動化、資料與備份、PDPO、接駁與金鑰 |
| 教學（唯一入口） | `#/docs` | 角色快速入門（旅長／教練員／家長／支部人員）、模組說明、開旅 checklist、**功能藍圖**；另有「教學係咩？喺邊？」解答 |

**免登入／外部頁面**

| 頁面 | 網址 | 示範重點 |
|---|---|---|
| 公開頁 | `public.html` | 只列等級 0 嘅嘢；「想睇多啲」分家長／成員／物資三路 |
| 通告分享 | `notice.html?n=n-1` | 分享連結唔使密碼；報名（家長代填）、膳食／藥物備註、PDPO 同意、訂閱 |
| 通告失效 | `notice.html?n=nope` | **誠實失敗**：唔會靜靜地彈第二張通告頂替 |
| 邀請開戶 | `join.html?t=TROOP-COA-7F3A9C2E` | 有效 token → 開戶（教練員）；支部人員示範 `?t=TROOP-MEM-5C7D1F2A`（落該團 SHEET ＋ 填 YMIS／生日）；`?t=TROOP-MEM-9A1C3E5G`（已用）／`?t=TROOP-PAR-2B8D4E6F`（過期）／`?t=NOPE`（無效）各有專屬畫面 |
| 物資借用 | `borrow.html` | 免登入只列「全生態共享」；切「生態內帳號」多見到「旅內共享」；旅部物資即時通過、支部物資待批 |

---

## 3. 一定要睇嘅「死規矩」示範（呢啲最易做錯）

1. **單一寫入掣**：改任何嘢只會變「N 項未寫入」，要撳頂部「儲存到後端」；收據一定要有 `confirmed:true`（寫完讀返自證），冇就當失敗。
2. **誠實失敗**：接駁燈三色（綠＝接駁好＋已閂本地登入／黃＝已登記未閂／紅＝未接駁）；後端實況如實報行數、壞表、後端版本。
3. **金鑰永不落地**：QR／連結、URL、前端碼永不見 apikey；變數 A／B／C／D 只喺 Vercel env 同 GAS ScriptProperties。
4. **冇 callback／冇定時器**：系統 →「自動化」寫明每項工作喺平台側（GitHub Actions／Drive／排程）執行。
5. **成員帳號住支部**：旅只做導流（旅閘「我係成員」→ 揀支部 → 轉去該團成員入口）；旅唔會、亦唔可以代驗成員密碼。
6. **未登記不得加導航**：導航由 `assets/js/lib/registry.js` 自動生成（最多兩層），模組開關喺系統 →「模組開關」。
7. **家長頁係旅專有**：支部系統冇家長頁（深資自務自治），所以「我的子女」只喺旅出現。
8. **未登記下游＝入唔到**：支部人員揀團頁會紅字講明（唔係壞咗，係未登記）；好過扮成功、扮有資料。
9. **超管隱藏，但入得晒**：帳號唔喺名單／統計／導航／邀請，入口要 `?step=super` 或撳 ⚜ 五下；
   但**唔經支部 SHEET 登記** → 未登記／紅燈／閂咗／接駁斷嘅支部佢都入得（紅色「超管視角」橫額每頁提醒）；
   平台有「★ 超管救援」可以**重設旅長／ADMIN 密碼**（第二層）。
10. **旅入口＝支部入口**：支部人員由旅閘登入之後**直接企喺自己支部**（`session.landedIn`）；換團＝返旅閘揀另一個團，唔使搵第二個網址。
11. **分享嘅嘢唔會自動出現**：未接收只喺分享中心；接收咗先混入清單（標明來源）；退回會留低理由。
12. **🆘 求救制**：入唔到／有咩問題 → 撳求救（**免登入**）→ 旅部 ADMIN 喺「待辦與批核 → 🆘 求救」或支部頁處理：
    **開返支部系統登入**（sig）／**重設密碼**（臨時密碼，首登強制改）／**答覆並結案**。
    求救**唔會自動開任何嘢**（唔然打幾個字就入得），ADMIN 一定人手核實身份先做。
    示範：樂行童軍團團長求救（被閂入唔到）／家長求救（未接駁）／副隊長求救（唔記得密碼）。
    留意：**支部系統前台永遠冇「開返／解鎖」掣**，得「🆘 求救」；本地領袖戶閂咗一樣 403。

---

## 4. 功能齊備度點對照

App 內已經有一頁「**功能藍圖**」（教學 → 功能藍圖，或 `#/docs/blueprint`）：

- P0 骨架・登入・接駁（9 項）
- P1 跨支部可見・家長・通告訂閱（7 項，其中 6 項 UI 已做，1 項等團側）
- P2 整合（財務／物資／移交，5 項）
- P3 平台與教材（5 項）
- **唔係旅做**（4 類）：會議／點名／試卷／支部明細 → 只跳轉；進度 UI → 豁免；相片上載／圖書館 → 唔做
- **等你定案**（6 項 §13 開放問題，UI 全部用「建議默認」實作並標明）

---

## 5. 已知未做（唔想呃你）

| 項目 | 狀態 | 幾時做 |
|---|---|---|
| `/api/*` | ✅ 已寫好 **9 支**（proxy／auth／super／units／downstreams／troop／registry／member-entry／share；本機 18 項測試綠）；**未上真 Vercel** | 跟 [後端部署步驟](後端部署步驟.md) 放 env |
| 旅 `Code.gs`（白名單 action、ScriptLock、逐表寫自證、sig、掣、匯出匯入） | ✅ 已寫好（本機 23 項測試綠）；**未上真 Sheet** | 同上 §1–§2 |
| `/api/troop`（分層 cache 5／30 分鐘）／`member-entry`／`share`／`registry` | ✅ 已做（P1 · 2026-09-26） | 真環境實測 |
| 真 sig 讀寫（路 S） | 旅側已實作；未同真下游對打 | 等團側補 `handleSignedRequest` |
| 支部系統 403 頁嘅「🆘 求救」掣 | 屬團側（旅側 UI 同求救頁已做好） | 團側照抄一條連結（§4.5.1） |
| 真密碼驗證（PBKDF2 100k）／session | ✅ `api/auth.js` 已做（前端示範模式照舊 demo1234） | 真環境用 §3 設密碼 |
| Drive 備份 | ✅ `Code.gs backupToDrive`（建立即 PRIVATE）；13 份輪替未做 | 排程屬平台側 |
| 版本樂觀鎖／backoff 重試／離線隊列 | ✅ `assets/js/lib/offline.js`（三色燈＋`baseVersion`＋唔自動覆蓋） | — |
| 大庫分件／tombstone purge | ✅ `saveDbPart`／`deleteRow`＋`purgeTombstones`（90 日） | — |
| merge3 欄位級合併 | ✅ `offline.js merge3／merge3Batch／merge3Rows`（同一格衝突逐格 ask；批量 serverTime 新者勝＋留底） | — |
| 匿名可寫面（報名／借用／收支／進度／開戶） | ✅ GAS 白名單 action ＋限流＋待批表＋去重；批／拒（拒要原因） | 真環境實測 |
| 教材三層 | ✅ `docs/教材/`（9 份）＋UI 對照表（smoke 驗冇死連結） | — |
| 成員端（各團 `members.html`） | 屬各支部 repo | 各團自己 |

## 5b. 真模式對接（P0 已寫好）

`npm run dev` 起嘅**本機 server 會直接行真 handler**（唔係另一套）：`/api/units`（公開清單＋`?diag=1`）、
`/api/downstreams`／`/api/auth`／`/api/super`（env 唔齊＝誠實回 `not_configured`，唔會扮有）；只有 `issue` 係本機 sink。

| 件 | 位置 | 做咩 |
|---|---|---|
| 前端唯一寫入掣 | `assets/js/main.js` ＋ `assets/js/lib/api.js` | 真模式（`_mock` 冇咗）→ `POST /api/proxy action=saveTables`（**只寫有改嘅表**）→ 讀返自證 → 真收據；示範模式零 fetch |
| 旅 SHEET | `apps-script/Code.gs` | router／apikey＋sig 雙通道／白名單／ScriptLock／逐表寫自證／審計鏈／限流／帳號下限／registry／出站 sig／匯出匯入 |
| 平台 | `api/{proxy,auth,super,units,downstreams}.js` | proxy 白名單轉發（inject apikey）／PBKDF2 100k 登入／票據／公開清單 |
| 部署 | [docs/後端部署步驟.md](後端部署步驟.md) | ADMIN 人手步驟 ＋ 12 項驗收清單 |

## 6. 自動驗證（你自己都跑到）

```bash
npm run lint     # 語法、import/export 對唔對得上、註冊表規矩、CSS class 有冇定義、體積、金鑰／localhost 禁區
npm run smoke    # jsdom（55 場景）：旅閘四條身份路、8 個視角（旅長／教練員／家長／副隊長／團長／團員／超管／訪客）
                 #        × 全部路由、16 模組 × 全部分頁、全部掣逐個撳（連 async handler）、
                 #        公開頁 5 種 token 狀態、報名／借用／開戶／財務確認／物資批核／移交接收（冪等）／
                 #        ★ 分享：未接收唔會出現 → 接收後出現＋標來源 → 退回留理由 → rank 2 只可加註解
                 #        ★ 旅入口＝支部入口（登入即入自己支部）／超管隱藏／未登記下游擋住
                 #        ★ 求救制（免登入送出 → 旅側處理：開返閘／重設密碼／答覆結案；唔會自動做任何嘢）
                 #        ★ 超管唔經支部登記 → 任何支部／模組都入得（未登記／紅燈／閂咗都擋唔住）
                 #        ★ 前端 ↔ 後端通道：示範模式一律唔發請求（`code:'mock'`）；表名同 Code.gs 分頁對得上
                 #        ★ 匿名可寫面：四個公開頁（public／notice／borrow／join）真模式經同源 /api/proxy，
                 #          前端／QR／URL 永遠唔見旅 GAS URL 同 key；三邊白名單（前端↔proxy↔GAS）要一模一樣
                 #        ★ 備份（Drive 留 13 份／三時機提醒／匯入前警告）＋PDPO（離隊 12 個月、數據清單、同意欄）
                 #        ★ 平台開旅：scripts/units.mjs（units.json ＋ env；檔永遠唔含 key／URL）
                 #        ★ 同步引擎：三色燈／樂觀鎖／merge3 逐格問（同格衝突唔自動揀）／batch serverTime 新者勝留底
                 #        ★ session 靜默刷新：剩 <10 分鐘自動續期（上限 8 小時）＋收到 401 續期一次再重試
                 #        ★ 忘記密碼：一次性連結（防枚舉、30 分鐘、用完即廢；寄唔到就交 token 人手傳）
                 #        ★ 子女綁定：家長申請 → **該團領袖確認先睇到**（名冊對唔上唔會批；唔會動子女個戶）
                 #        ★ backoff＋jitter：隊列記住試過幾次／下次幾時試；未夠鐘唔敲後端，人手撳＝即刻試
                 #        ★ 審計只記 metadata：長文字（內容）唔入 log，只記 len；email／電話遮中間
                 #        ★ 讀取樂觀化：讀唔等寫鎖；pointer 覆查（唔穩就老實講「讀取期間有人寫入」）
                 #        ★ sig jti：一次性簽名（cache ＋持久環），重放即拒；下游 zero 改動
                 #        ★ db shard：件數上限、寫入前後 bump 版本、分件失敗回滾（系統→後端有真檢查掣）
                 #        ★ 下游接入範本：apps-script/Downstream.gs（sig／閂口／leaf 自製 session token）
                 #        ★ 個人化訂閱：sw.js ＋ 真訂閱四步 ＋ /api/push 匿名轉發閘（未開通＝誠實講）
                 #        ★ 樂觀鎖：版本由 server 派；寫入帶 baseVersion，撞版＝conflict（自動重讀重合併一次）
                 #        ★ 移交：移出（TRANSFERRED_OUT tombstone）／套裝 sha256（canonical 欄序）／接收四道閘
                 #          （hash／transferId／冪等／撞號）／家長（同旅零改動 vs 轉旅停用＋邀請重開）
npm run gas-test # 旅 GAS：假 Apps Script 環境 38 項（sig 防護／逐表自證／審計鏈／hash 唔外洩／帳號下限／限流／
                 #             匿名可寫面＋限流分族／備份 13 輪替／PDPO 離隊 12 個月匿名化（預設演練））
npm run api-test # /api：PBKDF2 100k／session 防篡改／票據防重放／白名單／唔外洩 apikey／匿名面過閘（回歸）
npm run units -- list   # 平台開旅工具（data/units.json ＋ Vercel env 清單；check 會擋 key 入檔）
npm run check    # 以上全部
```

現況（2026-09-26 · P14）：`lint` 全綠（56 檔 · 1106 KB）、`smoke` **92/92**、旅 GAS 本機測試 **53/53**、`/api` 測試 **27/27**；
教材（角色 ×5／模組／示範任務／開旅 checklist／開戶與批核）落 `docs/教材/`，UI「系統 → 教學」有同一份對照表。
